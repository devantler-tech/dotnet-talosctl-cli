name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@520deec4d29878861a424a93f35ac7b310406380 # v1.11.7
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
